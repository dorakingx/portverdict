export default function HomePage() {
  return (
    <main className="shell">
      <p className="eyebrow">Behavioral parity, tested—not assumed</p>
      <h1>A migration can compile—and still break behavior.</h1>
      <p className="lede">
        PortVerdict puts competing Nemotron migration branches on trial and ships only the candidate
        backed by executable evidence.
      </p>
      <div className="status-card" role="status">
        <span className="status-dot" aria-hidden="true" />
        Foundation initialized. The complete evidence workflow is under active construction.
      </div>
    </main>
  );
}
